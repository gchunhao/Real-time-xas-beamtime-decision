# External P K-edge reference library

Reference spectra are deliberately external to application code. Put data files in this directory and list them in `references.yaml`. The application exposes the manifest through the profile interface; v0.1 does not perform automatic reference fitting.
